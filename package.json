{
  "name": "ai-quiz-generator",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "echo 'Use Vercel CLI for local development: vercel dev'",
    "build": "echo 'Vercel will build API routes automatically'",
    "start": "echo 'No start script'"
  },
  "dependencies": {
    "@google/genai": "^1.20.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.9",
    "@vercel/node": "^3.2.0",
    "typescript": "^5.5.2"
  }
}
